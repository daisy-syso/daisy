<%= simple_form_for @editor, wrapper: :common_form do |f| %>
  <% if @editor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@editor.errors.count, "error") %> prohibited this editor from being saved:</h2>

      <ul>
      <% @editor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :email, label: '邮箱' %>
  <%= f.input :username, label: '用户名' %>
  <%= f.input :password, label: '密码' %>
  <%= f.input :telephone, label: '电话' %>
  <%= f.button :submit %>
  <%= link_to '返回', editors_path, class: 'btn btn-info' %>
<% end %>
