<%= partial 'templates/details/header', locals: { title: "病因" } %>
<div ng-repeat="detail in data.disease_details">
	<p class="pre" ng-show="{{detail_id == detail.id}}" style="position:absolute;padding:0 10px;top:35px;bottom:40px;font-size: 1.2em;overflow-y: auto;">
		{{detail.detail}}
	</p>
</div>